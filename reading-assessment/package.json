{
  "name": "reading-assessment-prototype",
  "version": "1.0.0",
  "description": "Speed Learning - Reading Assessment Prototype",
  "scripts": {
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "cd backend && npm run build && cd ../frontend && npm run build"
  },
  "keywords": ["reading", "assessment", "speed-learning"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
